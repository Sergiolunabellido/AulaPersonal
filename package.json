{
  "name": "aulapersonal",
  "main": "electron/main.js",
  "version": "1.0.0",
  "description": "Aula Personal - Productivity Suite",
  "scripts": {
    "start": "node scripts/start-dev.js",
    "build:backend": "node scripts/build-backend.js",
    "build:ollama": "node scripts/build-ollama.js",
    "build": "npm run build:backend && npm run build:ollama && node scripts/verify-packaging-resources.js && npx electron-builder",
    "build:linux": "npm run build:backend && node scripts/prepare-linux-resources.js && node scripts/verify-packaging-resources.js --linux && npx electron-builder --linux dir && node scripts/zip-linux-dist.js",
    "build:win": "npm run build:backend && npm run build:ollama && node scripts/verify-packaging-resources.js && npx electron-builder --win",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "electron": "^42.4.0",
    "electron-builder": "^25.1.8"
  },
  "dependencies": {},
  "build": {
    "appId": "com.aulapersonal.app",
    "productName": "Aula Personal",
    "npmRebuild": false,
    "directories": {
      "output": "dist"
    },
    "files": [
      "electron/**/*"
    ],
    "extraResources": [
      {
        "from": "build/libs/AulaPersonal-0.0.1-SNAPSHOT.jar",
        "to": "backend/AulaPersonal-0.0.1-SNAPSHOT.jar"
      },
      {
        "from": "build/jre",
        "to": "jre"
      },
      {
        "from": "build/ollama",
        "to": "ollama"
      }
    ],
    "win": {
      "target": "nsis"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true
    },
    "linux": {
      "target": ["dir"],
      "category": "Office",
      "icon": "electron/renderer/assets/imagenes/mobile_profile.png"
    }
  }
}
